// Bar placement orchestration — §F
// Calls WASM solvers for face sampling, spacing, collision detection

export function placeBarsOnFace(
  _faceId: string,
  _diameter: number,
  _spacing: number,
  _cover: number,
): string[] {
  throw new Error('Not implemented — see M0')
}