// Drawing-layout constants for the 2D section view. The panel SIZE is a design
// token (tokens.css); these are canvas drawing values, like the viewport's
// interaction constants.

/** px — auto-fit margin between the drawing bounds and the canvas edge. */
export const SECTION_FIT_PADDING_PX = 16;
