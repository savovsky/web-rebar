# M2 Test Scenarios — Adapters Round-Trip (IFC + DXF)

> **Back to:** [Test Scenarios](./README.md) · [M2 tracker](../implementation-plans-and-tasks/m2-adapters-round-trip.md)
> Created 2026-08-18 (T3 session) — persists the approved T1–T2.5 manual test lists (M2-S01…S03) and records T3's headless IFC round-trip acceptance (M2-S04). T4 (File menu + browser round-trip) and T5–T7 (DXF) extend this file.

---

### M2-S01 — Spike IFC file opens in an external viewer (web-ifc write gate)

**Covers:** T1 (Q1 decision gate, criterion iii) · **Status:** ✅ manual 2026-08-18 (Allplan 2022, after 3 exporter-convention fix iterations) · **Headless counterpart:** `src/io/ifc-write-spike.test.ts` (gate criteria i + ii)

- **Given:** the artifact `docs/test-fixtures/ifc/m2-t1-spike.ifc` (gitignored path may vary — regenerated by the spike test)
- **When:** opening it in an external IFC viewer (Allplan 2022 / BIMvision / Solibri)
- **Then:** the file imports without errors — one 4000 × 300 × 2800 mm wall starting at (1000, 500, 0) and one Ø12 two-segment bar (B500B); where the viewer shows property data, the two `Pset_WebRebar_*` property sets are visible; a bare `edmiImportStepFile (11108)` line in the Allplan import modal with no failing entity is accepted as a non-blocking reader notice

### M2-S02 — Exported project opens in Allplan with correct positions and elevations

**Covers:** T2 + T2.5 (coordinate-convention re-check post Z-up migration) · **Status:** ✅ manual 2026-08-18 (Allplan 2022) · **Headless counterpart:** `src/io/ifc-mapping.test.ts` (entity-graph assertions; the artifact writer regenerates the file)

- **Given:** the artifact `docs/test-fixtures/ifc/m2-t2-export.ifc` (regenerated by the test run — verbatim Z-up coordinates since T2.5)
- **When:** opening it in Allplan 2022
- **Then:** TWO walls import cleanly — one 4000 × 200 × 2800 at ground level running east–west, one 4000 × 300 × 2800 floating at +3000 mm elevation running north–south (the elevation/coordinate check: the second wall must float UP, not shift in plan; both walls stand, not lie flat) — plus a straight Ø12 bar and an L-shaped Ø16 bar (B500B, 25 mm cover); elements ignored/defective = 0

### M2-S03 — Z-up model space: viewport and tools behave unchanged after the migration

**Covers:** T2.5 · **Status:** ✅ manual 2026-08-18 · **Headless counterpart:** the full vitest suite (214 tests at T2.5) re-fixtured to Z-up

- **Given:** the app running (`pnpm dev`)
- **When:** orbiting the viewport; placing a wall (W); chaining a 3-click L-shaped bar (B) on a wall side face; cutting a section (S) across the wall; dragging the wall with the Move tool (M) and undoing (Ctrl+Z)
- **Then:** the grid lies in the ground plane and walls rise vertically; the status bar shows **X / Y** plan coordinates; the bar renders inside the concrete at cover with the hook going UP; the 2D section shows the outline + bar dot at the correct cover side; move drags wall + hosted bars live and one undo restores both — every M0/M1 workflow (M0-S01…S18, M1-S01…S17) behaves as before

### M2-S04 — IFC round-trip: export → import yields an identical model

**Covers:** T3 (§A milestone acceptance sentence 1) · **Status:** ✅ automated headless 2026-08-18 — no user-visible surface in T3; the browser round-trip manual check arrives with T4 · **Headless counterpart:** `src/commands/import-ifc.test.ts` (the acceptance probe) + `src/io/ifc-import.test.ts` (mapping level)

- **Given:** a model built through the app's command layer — a wall (plus an elevated one) and a bent bar placed at 25 mm cover
- **When:** the model is exported to IFC and the resulting file is imported into a fresh project
- **Then:** the imported model is identical — same entity ids, wall parameters and bar paths exactly equal, design intent (cover distance, host, steel grade, diameter) exactly equal; the import is ONE undo step (undo removes the whole import exactly, redo re-applies it); a file containing foreign entities (no design-intent property sets or unsupported element types) imports the supported ones and reports the skipped count; importing the same file twice, a bar whose host is missing, or a non-IFC file are rejected with a clear error and change nothing
