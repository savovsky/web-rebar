# ⚠️ TEMP session prompt — M2 T6.5 (IFC reference solids — foreign products import as render-only dummy solids, Q7)

> Point a fresh AI session at this file (or paste its content). **Delete this file once the T6.5 session has started** — the durable record is the M2 tracker. Created 2026-08-18 after **M2 T6** (background rendering + tracing snaps + Backgrounds panel + Import DXF ✅ — 312 tests green, shell 1,290.85 kB, dxf chunk 33.85 kB lazy; commit `3b2348a`). Branch `A_MVP_Scope_M2`.

I'm working on a browser-based reinforced concrete drawing app ("web-rebar").
Read C:\work\personal\projects\web-rebar\README.md first — it has session state
(M0 + M1 ✅; M2 T1/T2/T2.5/T3/T4/T5/T6 ✅ — all 2026-08-18; IFC round-trip proven
headless AND in the browser; DXF backgrounds render + snap in the viewport;
model space is Z-up right-handed mm, identical to IFC/DXF; branch
A_MVP_Scope_M2) and the Rules for Implementation Sessions.
Then read docs/08-architecture-spec.md (especially §B.2, §B.3 revised, §C incl.
the Q7 revision note, §D.4, §H.1 incl. the Q7-a typed-array bend, §N) and the
approved plan docs/implementation-plans-and-tasks/m2-adapters-round-trip.md —
including ALL task logs, especially T5 (ReferenceDocument model + commands),
T6 (ReferenceLayer pattern, Backgrounds panel, useReferenceSnapTargets
exclusion contract), and the Q7/Q7-a decision rows.

⚠️ FIXTURE CHECK FIRST: `docs/test-fixtures/ifc/2026.07.12 BE TP APP.ifc`
(the author's Advance Steel 2018 export, IFC2X3 SteelFabricationView, 11 MB,
4,008 products — gitignored, invisible to git status) must be present.
✅ Confirmed available 2026-08-18, but VERIFY — it is T6.5's real-file perf
probe subject. If missing → STOP and ask the author.

Task: implement the next ⬜ Pending task from the M2 tracker — **T6.5: IFC
reference solids — foreign products import as render-only dummy solids** (plan
§6.5, Q7 approved 2026-08-18; trigger: the Advance Steel export skipped
4,008/4,008 products correctly-but-uselessly):

- `src/io/ifc-solids.ts` — pure extraction (rule 2): web-ifc `LoadAllGeometry`
  → per-product flat meshes (positions/indices, composed placement transform
  applied, model mm, IFC per-part color) for every geometry-carrying product
  that is NOT an intent-carrying wall/bar (those stay editable via the T3 path
  — no duplication); products without geometry (openings, spatial structure)
  are ignored, not counted.
- Data model: `ReferenceDocument.source` gains `{ kind: 'ifc', fileName }`;
  the document gains a solids variant carrying typed-array meshes (Float32
  positions / Uint32 indices + per-part color). The §H.1 JSON-clean bend is
  already recorded (Q7-a, dated spec note) — no new spec change needed, but
  verify the note covers what you build.
- `importIfcModel` reclassification: entities WITH intent psets → editable
  model (unchanged — the round-trip probe must stay green); geometry-carrying
  products WITHOUT them → ONE reference document inside the SAME command
  (still exactly ONE undo level per import). `ImportIfcModelSummary` gains the
  reference outcome (solid/triangle counts) for the status-bar summary;
  whether `missingIntentPset` walls/bars fold into the solids is decided at
  implementation — record the choice in the task log. The T4 status-hints
  formatter needs the new summary fields (pure module, headless tests).
- Rendering: `ReferenceSolidsLayer` in Viewport3D — merged BufferGeometry per
  document, IFC colors at reduced opacity (token fallback), per-document
  visibility via the existing Backgrounds panel (already source-kind-agnostic);
  **excluded from `pickPointerWinner` by construction** (no handlers/userData —
  the T6 ReferenceLayer pattern) and from ALL computation (sections, snapping,
  validation). T6 finding: snap targets key on ReferencePrimitive kinds —
  solids must never become snap targets.
- Headless tests: synthetic foreign IFC (the T3 `ifc-test-fixtures.ts`
  builders) → expected reference document; own-export import → NO reference
  document; one-undo-level probe; triangle-count sanity. **Perf tripwire at
  the author's Advance Steel file scale** (4,008 products — the M1 T5 probe
  pattern): extraction time + render budget recorded in the task log.
- Bundle tripwire: defend shell **1,290.85 kB** (T6), dxf chunk 33.85 kB lazy,
  web-ifc chunks lazy, no INEFFECTIVE_DYNAMIC_IMPORT.
- Update scenario M2-S06 (marked SUPERSEDED BY T6.5) in the same commit
  (rule 7); task log + tracker row (commit `—`).

Work on branch A_MVP_Scope_M2, one task at a time: pnpm lint + pnpm test +
pnpm build green → changes + manual test list → I review and commit.
Do NOT commit anything yourself.

Manual (the T6.5 acceptance probe): import the Advance Steel IFC → the steel
structure renders in the 3D viewport → hide/show + remove via the Backgrounds
panel → one Ctrl+Z removes the whole import → our own export re-imported →
editable entities, no solids.
